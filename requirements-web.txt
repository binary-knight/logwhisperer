# LogWhisperer Web Dashboard Requirements
# These are in addition to the base requirements

# Web framework
Flask>=2.3.0,<3.0.0
Flask-SocketIO>=5.3.0,<6.0.0
Flask-Compress>=1.13,<2.0.0

# WebSocket support
python-socketio>=5.9.0,<6.0.0
eventlet>=0.33.0,<1.0.0  # For production WebSocket server

# Additional security
Werkzeug>=2.3.0,<3.0.0
itsdangerous>=2.1.0,<3.0.0

# For password hashing (already in base requirements but ensure version)
cryptography>=41.0.0,<42.0.0